The smaller stasis_impact_effect-raw image was upscaled using https://imgupscaler.com/
It made the image 4 times the resolution, but also returned it as a JPG. So I just saved it back to a PNG with GIMP and it looks to be the same visuals, but smaller file size.